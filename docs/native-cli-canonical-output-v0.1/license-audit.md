# Native CLI Canonical Output License Audit v0.1

This candidate adds no dependency, donor source, binary redistribution source or license obligation. It uses the Windows C runtime `_setmode`/`_O_BINARY` interface already available to the native Windows build and the repository's existing JSON encoder.

Zig 0.16.0 remains a build tool and is not added to the repository.

# Frontier Formation 2^3 Factorial Sandbox Surrogate Evidence Ledger v0.1

**Verdict**：`CANDIDATE / implementation complete / canonical CI unavailable`  
**Date**：2026-08-11

## Artifacts

- `src/frontier-generic-factorial-scorer.mjs`
- `src/frontier-formation-factorial-sandbox-surrogate.mjs`
- `tests/frontier-generic-factorial-scorer.test.mjs`
- `tests/frontier-formation-factorial-sandbox-surrogate.test.mjs`
- `examples/frontier-formation-factorial-sandbox-surrogate.mjs`
- `docs/FRONTIER_FORMATION_FACTORIAL_SANDBOX_SURROGATE_v0.1.md`
- `src/frontier-design-grammar-router.mjs` v0.2 generic-factorial route
- `src/frontier-research-index.mjs` exports

## Closed implementation obligations

- generic non-NIST `2^k` payload schema: IMPLEMENTED
- complete-cell coverage validation: IMPLEMENTED
- balanced replication gate: IMPLEMENTED
- pm1 orthogonal term/effect/SS computation: IMPLEMENTED
- preregistered target-term decisions: IMPLEMENTED
- Design Grammar router generic full-factorial route: IMPLEMENTED
- Formation `layout_topology × orientation × boundary_mask` 2^3 surrogate: IMPLEMENTED
- 128-row default design with nuisance blocks: IMPLEMENTED
- seven pressure worlds: IMPLEMENTED
- post-build payload tamper detection: IMPLEMENTED
- external/new-law/magic flags hard false: IMPLEMENTED

## Repository execution status

The repository contains 10 new automated checks across the generic scorer and Formation surrogate suites.

They are **not recorded as PASS here** because GitHub Actions is currently prevented from starting by the account billing/spending-limit condition observed on the preceding Frontier PR. Until an executable Node/CI run is available, the Integration Court verdict stays `CANDIDATE`.

## Evidence boundary

```text
externalRealityVerified = false
newNaturalLawVerified = false
magicVerified = false
```

A future sandbox PASS would validate protocol discriminability only. It would not establish that real spatial arrays, formations, symbols, aether, or magic produce physical effects.

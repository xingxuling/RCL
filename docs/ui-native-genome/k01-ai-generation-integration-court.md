# K01 Compiler AI_GENERATE Integration Court

**Court status:** `LOCAL_CANDIDATE_GITHUB_LINUX_WINDOWS_REPLAY_REQUIRED`

## Admission scope

The frozen K01 v0.2 contract injects three distinct mutations into the RCL-owned compiler opcode-lowering table: addition, less-than-or-equal and the greater-than-or-equal/fallback boundary. Three fresh ephemeral read-only Codex CLI sessions receive only mutated compiler sources, semantic intent and deterministic failure diagnostics. They cannot write the authoritative repository, see canonical compiler files, change tests or self-issue PASS.

The evaluator applied one Schema-bounded exact edit per session. All three candidates restored the exact canonical compiler source hashes and the same compiler artifact root. A shared native fixed-point run established byte-identical `C0 == C1 == C2` because the repaired candidate bytes were identical.

## Court roles

| Role | Effect on engineering |
| --- | --- |
| Founder Twin | Selected compiler-source repair as the K400 nearest single-gate closure. |
| 柳清莲 Gate | Kept K339 BLOCKED until an external GitHub receipt binds both required hosted jobs. |
| 洞哥 Grounding | Required effective negative controls and exact restored bytes, not narrative generation claims. |
| Product / UX | No user-facing product scope was changed by this evidence-only campaign. |
| Engineering / Code | Preserved RCL as canonical compiler owner; the AI proposes bounded text edits only. |
| Test / Security | Replays receipt roots, unique session identities, mutation hashes, read-only boundaries and fixed-point identity. |
| Release / Evidence Ledger | May admit only K339 `AI_GENERATE` after Linux candidate replay and Windows native self-hosting both succeed in one push run. |

## Current evidence

- independent trials: `3/3`
- unique generator sessions: `3/3`
- canonical compiler core restored: SHA-256 `f7750c3d6d97bdfa767c0fb5a0a381ff77695cbc4991f3722decd4445a76ceab`
- native compiler artifact: SHA-256 `00321946e2b4651b4a05b229e7ec650c76375b394afebbc89fb7e095fc28779b`
- shared fixed point: `C0 == C1 == C2`
- local receipt root: `45fdf969b54a300b63f1b2de441e133101c3c135d1faa03d246bde8934b3c85a`

## Boundary

This is bounded compiler repair evidence. It does not establish arbitrary compiler evolution, whole-runtime self-hosting, general autonomous software engineering, unrelated K400 cells or K400 completion.

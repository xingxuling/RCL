# K334 Compiler Agent Campaign v0.1

## Current verdict

`PASS_LOCAL_RECEIPT_GITHUB_AUTHORITY_REQUIRED`

This campaign freezes one bounded `compiler-runtime::agent` profile. RCL source owns action representation, deterministic deliberation, capability grants, budget and risk bounds, human approval, kill-switch behavior, bounded audit memory, observe-only behavior and commit. Existing JavaScript agent products are Donors and product organs only; no LLM or external tool runtime receives canonical semantic ownership or K400 credit.

## Reality Audit and No Silent RCL Bypass

- Existing RCL `Sequence`, recursion, comparison, Boolean and `choose` semantics express the complete bounded agent state machine without a new Core primitive.
- Donor advantages are goal-directed candidate selection, explicit capability grants, bounded resources, inspectable memory and authority-aware commit.
- Native `rclc` and `rclvm` execute the frozen profile; JavaScript is limited to the independent oracle and evidence harness.
- `RCL_GAP_K334_EXTERNAL_AGENT_IO_PROTOCOL` remains open for authenticated observations, external tool effects, durable memory, model-provider provenance, idempotency and crash recovery.

No execution-language workaround owns a canonical result. The current evidence does not claim that RCL executes an LLM, calls tools, accesses a network or operates autonomously in production.

## Frozen profile

- two eligible actions with deterministic utility/evidence/risk/cost scoring;
- exact capability match before consideration;
- budget and maximum-risk rejection before commit;
- human approval for protected actions;
- kill-switch before deliberation;
- observe-only selection with zero spend and no commit;
- bounded signed audit memory for accepted and rejected candidates.

## Nine non-compensatory gates

| Gate | Local result | Evidence |
|---|---|---|
| EXPRESS | PASS | all deliberation, constraint, memory and authority semantics are canonical RCL source |
| COMPILE | PASS | bootstrap and native compiler emit byte-identical RBC |
| LOWER | PASS | generic Sequence/control-flow bytecode; no agent-special opcode |
| EXECUTE | PASS_LOCAL | native VM completed 20/20 local rounds; hosted Linux/Windows replay is pending |
| CORRECT | PASS_LOCAL | independent JavaScript oracle matches every frozen projection |
| ROBUST | PASS_LOCAL | capability, budget, approval, selection and corrupt-RBC controls fail closed |
| PERFORMANCE | PASS_LOCAL | local P95 compile/execute/combined times remain under frozen budgets |
| AI_GENERATE | CANDIDATE | three unique read-only sessions restored exact canonical bytes; hosted replay is pending |
| EVIDENCE | CANDIDATE | rooted local receipts exist; exact GitHub Linux/Windows authority is pending |

K334 remains `UNTESTED` in the authoritative K400 matrix until the exact source commit passes both named GitHub jobs and a rooted authority receipt is checked in.

## Multi-civilization Integration Court

| Civilization | Artifact consequence |
|---|---|
| Founder Twin | selected reusable deliberation and authority semantics rather than a product-specific chat loop |
| 柳清莲 Gate | keeps local proof candidate-only and makes all nine gates non-compensatory |
| 洞哥 Grounding | requires native state inspection, a separate oracle and negative constraint paths |
| Product | exposes stable stop codes for capability, budget, approval, kill and risk denials |
| UX | preserves observe-only inspection and signed audit memory before any commit |
| Engineering / Code | reuses the generic repair harness while keeping canonical semantics in RCL |
| Test | covers positive selection, no-commit mode, four fail-closed constraints, semantic mutations and corrupted RBC |
| Security | external tools are impossible in-profile; capability, approval and kill-switch checks precede commit |
| Release / Evidence Ledger | requires exact GitHub focused and Windows job/step identities before admission |

The Integration Court verdict is `CANDIDATE_READY_FOR_HOSTED_AUTHORITY`, not K334 PASS.

## License and diff audit

No dependency, donor implementation, external model, dataset or infrastructure binary was added. New RCL source, fixtures, oracle code and evidence scripts are repository-authored. The diff is additive and scoped to K334 source/contracts/evidence/tests/scripts, workflow/package wiring, K400 builder and documentation.

## Claim boundary

No LLM reasoning, arbitrary planning, external observation ingestion, tool execution, network access, durable memory, background autonomy, production commit authority, unrelated K400 cell or K400 completion is claimed.

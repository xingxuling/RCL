# Frontier Aether Continuous-Field Sandbox Surrogate Evidence Ledger v0.1

**Verdict**：`CANDIDATE / implementation complete / canonical CI unavailable`  
**Date**：2026-08-11

## Artifacts

- `src/frontier-continuous-field-scorer.mjs`
- `src/frontier-aether-continuous-field-sandbox-surrogate.mjs`
- `tests/frontier-continuous-field-scorer.test.mjs`
- `tests/frontier-aether-continuous-field-sandbox-surrogate.test.mjs`
- `examples/frontier-aether-continuous-field-sandbox-surrogate.mjs`
- `docs/FRONTIER_AETHER_CONTINUOUS_FIELD_SANDBOX_SURROGATE_v0.1.md`
- `src/frontier-design-grammar-router.mjs` v0.3 continuous-field route
- `src/frontier-research-index.mjs` exports

## Closed implementation obligations

- continuous-field time-series payload schema: IMPLEMENTED
- distance / phase / shield coverage validation: IMPLEMENTED
- environment-channel residualization: IMPLEMENTED
- frozen target lag: IMPLEMENTED
- frozen distance scale: IMPLEMENTED
- frozen phase function: IMPLEMENTED
- frozen shield transmission: IMPLEMENTED
- permutation-based kernel association check: IMPLEMENTED
- no adaptive lag/phase/distance-scale search: IMPLEMENTED
- Design Grammar continuous-field route: IMPLEMENTED
- seven Aether surrogate pressure worlds: IMPLEMENTED
- payload root tamper detection: IMPLEMENTED
- external/new-law/magic flags hard false: IMPLEMENTED

## Repository tests present

New automated checks are included across two suites:

```text
frontier-continuous-field-scorer.test.mjs: 5 tests
frontier-aether-continuous-field-sandbox-surrogate.test.mjs: 6 tests
Total new checks: 11
```

They are not recorded as PASS in this ledger until an executable repository Node/CI run occurs.

The preceding Frontier PRs demonstrated that current GitHub Actions jobs are being rejected before steps start because of the repository/account billing or spending-limit condition. This is an execution-infrastructure blocker, not evidence that these new tests pass or fail.

## Design sanity check

Before opening the PR, the statistical structure was independently sanity-checked outside canonical repository CI across the declared negative/positive scenario families. This check is only design validation and is not substituted for the repository tests.

## Evidence boundary

```text
externalRealityVerified = false
newNaturalLawVerified = false
magicVerified = false
```

A future sandbox PASS would validate only protocol discriminability under preconstructed model worlds. It would not establish an external information-transfer medium, aether, magic, or a new natural law.

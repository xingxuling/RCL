# K336 Compiler Automation Campaign v0.1

## Verdict

`PASS_RECEIPT_REPLAY_GITHUB_LINUX_WINDOWS_NATIVE_AUTOMATION_AUTHORITY_BOUND`

This campaign freezes one bounded `compiler-runtime::automation` profile. RCL source owns task dependency gates, bounded retries, human approval, kill-switch behavior, audit order, commit and compensating rollback. The existing JavaScript experiment-automation adapter is a Donor and product organ only; it does not own the canonical semantics or receive K400 credit.

## Reality Audit and No Silent RCL Bypass

- Existing RCL `Sequence`, recursion, comparison, Boolean and `choose` semantics express the workflow state machine without a new Core primitive.
- The Donor advantage is the combination of dependency scheduling, retry ceilings, safety stops and compensation.
- Native `rclc` and `rclvm` execute the complete frozen state machine; no shell, child process, infrastructure tool or external action Provider is invoked.
- `RCL_GAP_K336_EXTERNAL_EFFECT_PROTOCOL` remains open for authenticated external effects, durable queues, wall-clock scheduling, idempotency keys and crash recovery.

No execution-language workaround was used. JavaScript remains an independent oracle and isolated evidence harness; it does not own workflow results or promotion authority.

## Frozen profile

- successful three-task workflow with one retry on each of the final two tasks;
- retry exhaustion at the exact `failures == maxAttempts` boundary;
- compensation of two completed tasks after failure;
- approval denial before a protected task;
- kill-switch before execution;
- invalid dependency rejection;
- dry-run planning with zero attempts and no commit.

## Nine non-compensatory gates

| Gate | Result | Evidence |
|---|---|---|
| EXPRESS | PASS | all workflow and governance semantics are canonical RCL source |
| COMPILE | PASS | bootstrap and native compiler emit byte-identical RBC |
| LOWER | PASS | generic Sequence/control-flow bytecode; no automation-special opcode |
| EXECUTE | PASS | native VM completed 20/20 rounds and exact hosted Linux/Windows replay |
| CORRECT | PASS | independent JavaScript workflow state machine matches every projection |
| ROBUST | PASS | dependency, retry, approval, compensation and corrupt-RBC controls fail closed |
| PERFORMANCE | PASS | P95 compile/execute/combined times remain under the frozen budgets |
| AI_GENERATE | PASS | three unique read-only sessions restored exact canonical bytes; hosted replay is bound |
| EVIDENCE | PASS | rooted local receipts and exact GitHub Linux/Windows authority are bound |

GitHub run `33135430077` bound focused job `98734185571` and Windows job `98734185483` for exact source commit `a989db99643c8a3f61d6f416c57e5887c275eae1`. Both named K336 steps and both parent jobs succeeded. Authority root: `406b0891c40b01d7ce84021e36b2a188cb32c72db173fc9711df2ad0af341ab7`.

## Multi-civilization Integration Court

| Civilization | Artifact consequence |
|---|---|
| Founder Twin | chose reusable governed workflow semantics rather than product-specific automation cards |
| 柳清莲 Gate | keeps local evidence candidate-only and all nine gates non-compensatory |
| 洞哥 Grounding | requires native rounds, independent oracle and failure-path state inspection |
| Product / UX | stable result codes and audit sequences make every stop reason inspectable |
| Engineering / Code | extracts a reusable independent-repair evidence harness without transferring semantic authority |
| Test | covers success, exact retry boundary, dependency, approval, kill-switch, dry-run, compensation and corruption |
| Security | external actions are impossible in-profile; approval and kill-switch fail closed |
| Release | requires exact GitHub focused and Windows job/step identities |
| Evidence Ledger | binds source, compiler, RBC, semantic state, runtime, repair sessions and future authority roots |

## License and diff audit

No dependency, donor implementation, external dataset or infrastructure binary was added. New fixtures and oracle code are repository-authored. The diff is additive and scoped to K336 source/contracts/evidence/tests/scripts, reusable evidence harnesses, workflow/package wiring, K400 builder and documentation.

## Claim boundary

No external action execution, production deployment, authenticated side effect, wall-clock guarantee, arbitrary DAG, durable scheduler, distributed queue, infrastructure automation, unrelated K400 cell or K400 completion is claimed.

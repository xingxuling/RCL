# Integration Court — K08-K Tensor AdamW Bridge v0.1

## Target ruling

Admit a bounded Tensor-connected AdamW execution candidate so the RCL-1B campaign no longer depends on Batch SGD for Tensor model training.

## Civilization verdicts

- **Founder Twin:** PASS — target is directly on the frozen RCL-1B critical path.
- **柳清莲 Gate:** PASS — canonical owner remains the existing RCL Optimizer Genome; no optimizer semantics are silently reassigned to Rust.
- **洞哥 Grounding:** PASS — hosted Ubuntu/Windows execution exists; scalar fixture and Tiny LM are both exercised.
- **Product civilization:** PASS / infrastructure — removes a mandatory pre-10M gate; no user-facing product claim.
- **UX / design civilization:** N/A — no interface or interactive flow changed.
- **Engineering civilization:** PASS — additive binary/contract/tests; bounded resource and state validation; clear rollback.
- **Code civilization:** PASS — generic Tensor parameter/gradient/state bridge; no model-special opcode.
- **Test civilization:** PASS — positive, differential, checkpoint, deterministic, negative and real Tiny-LM coverage.
- **Security civilization:** PASS — deny-unknown request schema, finite-value validation, work ceilings, state binding checks and mutable-storage alias rejection.
- **Release civilization:** PASS FOR CANDIDATE — dedicated cross-platform workflow is green; large-scale claims remain closed.

## Evidence

Admitted hosted replay: GitHub Actions `32842406995`.

- Ubuntu `97784604949`: PASS.
- Windows `97784604602`: PASS.
- focused suite: 7/7 per host.

Initial run `32842289192` is retained as negative evidence. It exposed the existing scientific-number state-root formatting gap when `1e-8` was exported as a final RCL state facet. The repair kept strict root verification and the exact epsilon value; only unnecessary state exposure was removed.

## Authority boundary

The Court grants only:

`ENGINE_E3_TENSOR_ADAMW_BRIDGE_CANDIDATE_GITHUB_REPLAY_BOUND`.

It does not grant tokenizer, RoPE, multi-head/GQA, multi-block scale, BF16, GPU/NPU, RCL-10M, DistributedTensor, RCL-1B completion or K400 promotion.

## Rollback

Revert the K08-K campaign commits. Existing Tensor, Autodiff, scalar Optimizer Genome and Tiny LM Batch-SGD path remain unchanged because the new AdamW execution organ is additive.

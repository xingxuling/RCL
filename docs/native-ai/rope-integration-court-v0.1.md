# K08-N RoPE Integration Court v0.1

## Founder Twin target

Close the RCL-1B positional-semantics gate without adding a GPT/Transformer-specific core operation.

## 柳清莲 Gate

PASS for bounded candidate admission. RCL owns position/pair/angle/lowering semantics. Trigonometric table materialization remains an execution organ.

## Grounding Court

PASS.

Hosted evidence shows position-zero identity, independent angle differential, norm preservation, reverse-mode gradient parity, Q/K attention integration and strict negative boundaries.

## Product civilization

PASS / model infrastructure. This removes the zero-position placeholder used by the Tiny LM lineage and establishes reusable sequence position semantics for later multi-head/multi-block models.

## UX / design civilization

N/A. No user-facing surface changed.

## Engineering civilization

PASS.

The key boundary is intentionally small:

`RCL RoPE semantics -> rooted position frame -> generic Tensor matmul/mul/add -> existing Autodiff`

No new Tensor backward rule and no `rope-special` opcode are required.

## Code civilization

PASS. Position-frame root binds semantic configuration while transcendental numerical tables are treated as backend numerical materialization and verified by tolerance rather than falsely claiming universal bit identity.

## Test civilization

PASS.

Run `32847032779`:
- Ubuntu `97799011477`: 7/7 PASS.
- Windows `97799011150`: 7/7 PASS.

## Security civilization

PASS for bounded semantic/input integrity. Odd head dimensions, invalid base, zero sequence and overflowing position ranges fail closed. Model-special Tensor operation injection is rejected.

## Release civilization

PASS as candidate-only infrastructure. Rollback is additive and leaves the existing one-head zero-position Tiny LM path available for historical reproduction.

## Evidence Ledger verdict

`ADMIT_ROPE_GENOME_CANDIDATE_GITHUB_REPLAY_BOUND`

## Next court target

Parametric multi-head/GQA composition with RoPE applied inside Q/K head dimensions, followed by multi-block model construction.
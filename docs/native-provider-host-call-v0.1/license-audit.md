# Native Provider Host Call License Audit v0.1

This candidate adds no external package, copied donor source or new license obligation. It changes RCL-owned JavaScript, C, tests, examples, generated native artifacts and evidence documents under the repository's existing license.

The implementation reuses the repository's Provider ABI v1, RBC opcodes, SHA-256 implementation and build scripts. Zig 0.16.0 is a build tool only and is not redistributed by this candidate.

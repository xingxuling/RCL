{
  "schema": "rcl.compiler-microbench.workload.v0.1",
  "id": "utf8-byte-length-v1",
  "inputs": {
    "payload": "RCL现实"
  },
  "expected": {
    "outputType": "number",
    "resultOutput": 9
  },
  "benchmark": {
    "compileIterations": 3,
    "runtimeIterations": 12
  },
  "contract": {
    "sameInputRootRequired": true,
    "correctness": "encode the declared text as UTF-8 and return its byte length",
    "execution": "compile the declared source and execute its emitted artifact"
  }
}
